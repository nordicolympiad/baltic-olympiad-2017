INT(2, 2000, n)
SPACE
INT(2, n, k)
NEWLINE

REPI(i, n, NEWLINE)
  REPI(j, n, SPACE)
    INT(0, 1, x)
    IF(i == j)
      ASSERT(x == 0)
    END
  END
END
NEWLINE

REPI(i, k, SPACE)
  INT(0, n-1, a[i])
END
ASSERT(UNIQUE(a))
NEWLINE
EOF
